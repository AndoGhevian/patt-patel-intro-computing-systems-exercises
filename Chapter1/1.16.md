### 1.16 [1] Name at least three things specified by an ISA.

1. Instruction set: which is operations that computer can perform and where he should get values on which to perform operations.
2. data types: which is representations of values so that computer can perform operations on them
3. addressing modes: which is a mechanism that specify
where values used can be located, and how to specify that
locations to use by a computer when performing instruction.
