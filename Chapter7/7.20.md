### 7.20

#### a.

This program should be executed after assembly
to store x0015 to memory location x4000

#### b.

After object file or executable image
is loaded in to memory, as specified by
assembly pseudo-ops memorylocation
x4000 already filled with value x0015
before even a single machine instruction
of ISA is executed at all.

#### conclusion

So fundamental difference is that first program
should execute machine isntructions to achive that,
the second one already contain memroy x4000
with value x0015 before anything is executed.
