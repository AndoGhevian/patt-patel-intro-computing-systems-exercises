### 7.33

```
            .ORIG x3000
            LD R0, A
            LD R1, B
            NOT R1, R1
            ADD R1, R1, #1
            AND R2, R2, #0
AGAIN       -------------- (a)
            -------------- (b)
            BRnzp AGAIN
DONE        ST R2, C
            HALT
A           .FILL #5
B           .FILL #2 (c)
C           .BLKW #1
            .END
```

|Cycle Number| State Number | Information
|---|---|---
11|27|LD.REG: 1, DRMUX: 000, GateMDR: 1, LD.CC: 1, GateALU: 0, GatePC: 0
16|30 (fix 35)|LD.MDR: 0, MDR: x2209, IR: x2009, LD.IR: 1
50|1|LD.REG: 1, MDR: x14A1, DRMUX: 000, BUS: x0001, GateMDR: 0
57|1|PC: x3006, IR: x1040, GateALU: 1, BUS: x0003, GatePC: 0
65|22 (fix 23)|ADDR1MUX: PC, ADDR2MUX: PCoffset9, LD.PC: 1, PC: x3008, PCMUX: ADDER

57 cycle:

initially R0 = #5

x1040 = 0001 000 001 0 00 000 = ADD R0, R1, R0

BUS is #3 so result of addition is #3.

R0 = #3 = R1 + R0

last cycle:

After this instruction which could be at location a) or b)
we have LD.PC = 1 so, it is deffinitely not state 23 but
some state that modidy PC using ADDER.

50 cycle:

x_4A_ = ???? 010 010 10 => ARITHMETICOP R2, R2, 0????

is a) or b)

R2 <- x0001

next instruction will start at 50 and if memory access take
1 clock cycle could finish at minimum at 56, but because of the fact that that 57 is ADD first state after decode, memory
access should take exatcly 2 clock cycles.

so we have:
1. a) ARITHMETICOP R2, R2, 0????
2. b) ADD R0, R1, R0

So the only case when PC is loaded using ADDER is
last BRnzp AGAIN instruction at state 22, not 23 as in the
table of a book.

16 clock cycle

Here we have second LD, because LD complete in 11 clock cycles.

first raw could be completed by anything approrpaite, but when we try to fill it with some state of first LD, it seems
second raw state 35 is incorrect here, and it is other.

even clock signals talk about this incorrectness, there is
no need in ACV for any of provided to fill.
clock cycle 16 is state 30 loading IR.

lets create symbol table:
1. AGAIN -> x3005
1. DONE -> x3008
1. A -> x3010
1. B -> x3011
1. C -> x3012

1. 0010 000 000001001 = x2009
1. 0010 001 000001001 = x2209


Now lets see when (by clock cycle) what changes are made to registers and try to undertsand what is its result.

well considering these two we can find out that arithmetic op is ADD and not AND, (no way to get x0001 by anding 0 which is initial value of R2):
1. a) ARITHMETICOP R2, R2, 0????
2. b) ADD R0, R1, R0

ADD R2, R2, 00001

also it is obvious that it will be an infinit loop, we need a
sentinel check, that wehn result of R0 <- R0 - R1, where:
1. R0 init A
1. R1 init B

gives negative or zero loop stops.
so wee need to add at lable AGAIN check of
` BRnz DONE`

So obviously it is first time that add is performed
at clock cycle 50 to make R2 <- #1

So after that at clock cycle 57 it is the first time of
second ADD.

which result in R0 <- #3 = #5 - ? => ? = #2

NOTE: There are errors with book answer, it has some incorrect
parts - not providing notes.
