### 7.29
before breakpoint:

xBFFE = 1011 111 111111110 = STI R7, #-2 => Mem[Mem[PC - 2]] <- R7 =>

1. Mem[Mem[x17FE]] <- R7
1. Mem[x17FE] = x7FFF
1. Mem[x7FFF] = x2211
______
1. A = x1801
1. B = 
1. C = 
1. D = x1802
1. E = 
______
1. F = x1800
1. G = G is written to Mem[x1800]
1. H = x1803
1. I = x?F??
1. J = G See below

because of the fact thet Mem[F = x1800] = J affect R4 and not R5 like the
first time, so there is instruction which modifies Mem[x1800], which is
the second instruction:
1. first modify R5
2. thrid has in MAR D which is x1802, so it is not

So we have E as STR or ST instruction which modifies
contents of x1800 to G based on registers R0->R7

Now lets see which of register contents R0->R7 modify R4 x21FF -> x223A

1. R0 = x31FF => 0011 0001 => ST R0, not case
1. R1 = x2233 => 0010 => LD, not case
1. R2 = x5177 => 0101 0001 => AND R0, not case
1. R3 = x3211 => 0011 0010 => ST R1, not case
1. R4 = x21FF => 0010 0001 => LD R0
1. R5 ?
1. R6 = x3177 => 0011 => ST, not case
1. R7 = x2211 => 0010 0010 LD, not case

So it is R5 value C which is some instruction whihc modify R4 from
x21FF -> x223A

1. A = x1801
1. B = 
1. C = 
1. D = x1802
1. E = 
______
1. F = x1800
1. G = C => Mem[x1800] <- C
1. H = x1803
1. I = x?F??
1. J = G = C

Because "I" is modfiy PC from x1803 to x1800, and because after opcode
we have 4 bit 1's -> 1111, it cant be JSRR and JMP, maybe
unconditional branch or JSR, well JSR will modify R7 with PC, but it is not the case, because R7 is not modified by third instruction.

So it is unconditional jump from x1803 to x1800:

I = x0FFD

1. A = x1801
1. B = xEA66
1. C = x1867 (see below)
1. D = x1802
1. E = x3BFE (See below)
______
1. F = x1800
1. G = C => Mem[x1800] <- C = x1867 (see below)
1. H = x1803
1. I = x0FFD
1. J = G = C = x1867 (see below)

E is Store in x1800 the cotents of R5.

ST R5, #-2,

we have: E = 0011 101 111111110 = x3BFE

Well to modify R4 we need LD, LDR, ADD, AND, NOT, LEA.

x21FF -> x223A = 0010 001 000111010 = LD R1, #58

ADD is most appropriate to use with R1, because anyway there is always a possibility to use LD or LDR, because none knows what content is inside
memory.

So lets use ADD with R1 = x2233 to get x223A

instruction => 0001 1000 0110 0111 = x1867

x1867 is best to get as a PC-relative address with LEA

x1801 + x0066 = x1867, so we have in B:

1110 1010 0110 0110 = xEA66

In answers there is B incorrect, because they dont count from
incremented PC, which is actually PC-relative address.
