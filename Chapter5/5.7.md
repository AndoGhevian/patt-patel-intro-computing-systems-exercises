### 5.7 What is the largest positive number we can represent literally (i.e., as an immediate value) within an LC-3 ADD instruction?

ADD instruction has a form with second source operand as an
immediate value, that form reserves 5 bits for 2's complement integer data type. So we have 2^4 - 1 = 15 is the maximum
positive integer of this representation.
