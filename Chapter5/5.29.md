### 5.29

a.

1. LDR SR, SR, #0
1. STR SR, DR, #0

b.

We can computer BASE + SEXT(0) by using ADDR2MUX select for
input of value 0 for offset and ADDR1MUX to select SR1.
And immediately assert appropriate MARMUX line with GATEMARMUX
tristate device.

All this can be performed in one machine cycle.

So here are microinstructions:
1. MAR <- SR
1. MDR <- Mem[MAR]
1. MAR <- DR
1. Mem[MAR] <- MDR
