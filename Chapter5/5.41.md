### 5.41

Decoder in figure gives 1 for opcodes:

0000, 0101, 0010, 1010, 0110, 1110, 1001.

These are:

BR, AND, LD, LDI, LDR, LEA, NOT

As i see the logic connected with global bus relates to final step of
loading CC with instruction result, particularly,
it gives 1 if bit[15] is 0 and there is at least 1 bit contain
1 in it, i.e. result is positive.

It only takes effect for given opcodes, but, there are invalid
2 of them -> LEA and BR.

Also ADD opcode should be added.
