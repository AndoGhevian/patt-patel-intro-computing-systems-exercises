### 5.45

PC cant be directly connected to Memory, there is
a special Memory access register MAR to contain
address to read, in MAR <- PC case, to read current
instruction to execute.

After that PC must point to next instruction for next
fetch phase start fetching it directly, so we increment it.

Where to increment is depend on conceptual bounding. It is better
to increment at fetch phase.
