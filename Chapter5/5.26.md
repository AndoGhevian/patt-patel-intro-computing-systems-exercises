### 5.26 Your task is to consider the successor to the LC-3. We will add ten additional opcodes to the ISA and expand the register set from 8 to 16. We will change the memory to byte-addressable, with total address space of 64K bytes. Instructions will remain 16 bits wide. Also, we will encode all instructions, both old and new, with the same fields as the original 15 instructions, although we may need to change the size of some of the fields.

a. Is there any problem completing the detailed specification of the successor to the LC-3, as described above? Explain.

Well lets calculate how we will fit all this within 16 bits
of instruction.

25 opcodes can fit in 5 bits, 16 register identifier field needs
4 bits, now, for as it say, we will encode all old and new instructions with same fields, so we have:

5 (opcode) + 4 (dist operand) + 4 (source operand) and we have
rest 3 bits. now in 3 bits we can fit maximum 8 registers, so we
will support only old 8 registers for 2 register sources.

For form with immediate operand we will use other opcode,
even though for whole 16 register set support we need to
sacrifice imm3, which impractical range: -4, 3.

So we decide to support this form for 3 bit of register and
the rest 4 bits as immediate operand. -8, 7

For memory access, well, 16 bit GPR is enough to address
64K, so it is good.

For load/store operations we will have:

indirect mode we will have a limitation of a byte size address, first 2^8 addresses of an address space.

For PC-relative addressing mode, and indirect mode we will have
an address generation bits which is offset of size: 7 bits instead of 9.

For base+offset mode we will have:
will have base 4 bit field and offset 3. it can access any memory.

Memory should be organized such that for one address it will
access subsequent 2 bytes to fill 1 GPR, because of a usage
of Load instrctions with destination registers.

The same is for store, 2 contiguous memory locations will be filled.

Control Instructions will work as before.

b. How many bits do we need in the PC to be able to address all of memory?

16 bits are enough. 2^16 = 64 K

c. If we want 128 different operating system routines to be able to be accessed with a trap instruction and we form the address of each of these routines by shifting the trap vector to the left by five bits, what is the minimum amount of memory required by the trap service routines?

Mimum distance between 2 routines start instructions is 100000,
32 addresses, which 32bytes, now for 128 routines, which are
identifyable by 7 bits, for which out new instructions have
enough space, we need 2^7 * 2^5 bytes = 4KB space is required.

d. If, in the new version of the LC-3, we reduced the number of registers from eight to four and kept the number of opcodes at 16, what is the largest immediate value we could represent in an ADD instruction on this new machine?

we will need 2 bit register fields, so:

4 (opcode) + 2 (destination) + 2 (source) + 1 (mode identifier) + imm7

7 bits.
