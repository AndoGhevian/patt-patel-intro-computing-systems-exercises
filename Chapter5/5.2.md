### 5.2 A memory’s addressability is 64 bits. What does that tell you about the size of the MAR and MDR?

MDR will contain read data from memory location, so its size
should be 64 bits. MAR, which is contain address of memory
location to be accessed can be of any size, no assumtions
could be made.
