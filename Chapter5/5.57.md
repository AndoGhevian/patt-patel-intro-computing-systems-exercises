### 5.57

#### What opcodes use IR [11:9] as inputs to SR1?

Even though it is not obvious how SR1 applyed to BUS without adding to 0, or similiar operation, but it is used when we want
to use contents specified by IR.DR (IR[11:9]) as SR.

So we have: ST, STI, STR

NOTE: ALUK can be 11, which means pass input A.

#### Where does the control signal of this mux come from? Be specific!

It comes for instructions ADD, AND from bit IR[5]

#### What opcodes use this input to the MARMUX?

TRAP use it.
