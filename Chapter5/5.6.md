### 5.6 Recall the machine busy example from Section 2.6.7. Assuming the BUSYNESS bit vector is stored in R2, we can use the LC-3 instruction 0101 011 010 1 00001 (AND R3, R2, #1) to determine whether machine 0 is busy or not. If the result of this instruction is 0, then machine 0 is busy.

#### a. Write an LC-3 instruction that determines whether machine 2 is busy.

||||||
|---|---|---|---|---|
0101 |011| 010| 1|          00100
AND|   R3  |R2|  bit [5] |  machine 2 (index)


If result is 0 then machine 2 is busy

#### b. Write an LC-3 instruction that determines whether both machines 2 and 3 are busy

||||||
|---|---|---|---|---|
0101 |011| 010| 1|          01100
AND|   R3  |R2|  bit [5] |  machines 2 and 3

if 0 then both are busy.

#### c. Write an LC-3 instruction that indicates none of the machines are busy

because there are 16 machines identified by BUSYNESS bit vector, because of the fact that it first in GPR.

And also because all beasy means 2's ocmplement 1..1 of 16 bits,
which is -1 integer value, we can simply add imm5 00001 to it, and if result is 0, then all bits of BUSYNESS bit vector are
1's, and so all are free.

||||||
|---|---|---|---|---|
0001 |011| 010| 1|          00001
ADD|   R3  |R2|  bit [5] |

Or we simply can do AND with imm5 11111 and check if result is 16 bits 1, which is -1.

||||||
|---|---|---|---|---|
0101 |011| 010| 1|          11111
AND|   R3  |R2|  bit [5] |  All are free (which is 1)

#### d. Can you write an LC-3 instruction that determines whether machine 6 is busy? Is there a problem here?

We cant isolate bit 6 to AND it and check its value with imm value, because there are immediate 5 bits - [4, 0].

But we can use register with 16 bit value where only bit 6 is
isolated. But here we would need 2 instructions, one LOAD and one AND.

With ADD instruction there is always 1 bit is affected besides
bit 6, which is bit 5. So no way to isolate it eather.
