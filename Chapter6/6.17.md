### 6.17

PC is modified, so it is control instruction which successfully branch. Also R7 after that contains previous PC + 1, so it could be 0100 (JSR) instruction for calling routines.

There are no other instructions that affect R7 register after
they finish execution. So it is 0100.

now, because of how PC is incremented, we have:

JSR or JSRR.

because there is no register with new PC address x3220, it is
not obtained from register, but from sign extended offset.

SO it is 0100 1 00000001111 | JSR #15
