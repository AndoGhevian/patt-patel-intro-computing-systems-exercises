### 6.8

Well R2 is used to count the number of occrances of input character in
a file, so if we not initialize it to zero,
then it will ontain initially before any counting a garbage value,
which means after counting finishes, we will also have a garbage.
